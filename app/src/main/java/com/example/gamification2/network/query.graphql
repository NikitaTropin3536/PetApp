query GetWeatherNow(
    $latitude: Double,
    $longitude: Double
){
    weatherByPoint(
        request: { lat: $latitude, lon: $longitude }
    ) {
        now {
            temperature
            icon(format: SVG)
        }
    }
}